{
  "name": "scripts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup-vscode": "bun run src/setup-extension.ts --vscode",
    "setup-antigravity": "bun run src/setup-extension.ts --antigravity",
    "setup-all": "bun run src/setup-extension.ts --all"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
