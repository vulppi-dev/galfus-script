# galfus-builtins

`galfus-builtins` contains native implementations and bindings for the standard library (such as `std/io`).

## Responsibilities

- **Standard Library Modules**: Registers and exports core standard functions (e.g., printing or writing to terminal).
- **Built-in Symbols**: Exposes built-in function names and signatures to the resolver and type checker.
